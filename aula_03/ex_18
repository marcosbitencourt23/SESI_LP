#include <stdio.h>
int main() {
    int n, maior = -2147483648;
    while(1) {
        printf("Número: ");
        scanf("%d", &n);
        if(n == 999) break;
        if(n > maior) maior = n;
    }
    printf("Maior: %d\n", maior);
    return 0;
}