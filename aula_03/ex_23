#include <stdio.h>
int main() {
    int n, div = 0;
    printf("Número: ");
    scanf("%d", &n);
    for(int i = 1; i <= n; i++) if(n % i == 0) div++;
    if(div == 2) printf("É primo\n");
    else printf("Não é primo\n");
    return 0;
}