#include <stdio.h>
int main() {
    int n, cont = 0;
    while(1) {
        printf("Número: ");
        scanf("%d", &n);
        if(n < 0) break;
        cont++;
    }
    printf("Digitados: %d\n", cont);
    return 0;
}