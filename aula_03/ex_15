#include <stdio.h>
int main() {
    int n, soma = 0;
    do {
        printf("Número (0 para parar): ");
        scanf("%d", &n);
        soma += n;
    } while(n != 0);
    printf("Total: %d\n", soma);
    return 0;
}