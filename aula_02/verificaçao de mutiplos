#include <stdio.h>

int main() {
    int a, b;

    scanf("%d %d", &a, &b);

    if (a % b == 0) {
        printf("Multiplo\n");
    } else {
        printf("Nao e multiplo\n");
    }

    return 0;
}